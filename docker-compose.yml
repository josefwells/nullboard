version: "3"
services:
  nullboard:
    build: .
    image: jcurtis4207/nullboard
    ports: 
     - 20080:80
    ### chown www-data:www-data <path-to-volume>
    #volumes:
    # - <path-to-volume>:/var/www/html/boards
    container_name: nullboard
